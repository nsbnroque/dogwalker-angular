import { DiaSemanaPipe } from './dia-semana.pipe';

describe('DiaSemanaPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaSemanaPipe();
    expect(pipe).toBeTruthy();
  });
});
